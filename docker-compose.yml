version: "3.8"
services:
  mongo:
    image: mongo
    container_name: calendar-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: calendar
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
